name: lint

on:
  push:
    branches:
      - main
    paths:
      - "ddbj_search_converter/**"
      - "pyproject.toml"
      - "tests/**"
  workflow_dispatch: {}
  pull_request:
    types:
      - opened
      - synchronize

jobs:
  lint:
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup uv
        uses: astral-sh/setup-uv@v5
        with:
          python-version: "3.12"

      - name: Update and Install apt Dependencies
        run: |
          sudo apt update
          sudo apt install -y curl jq

      - name: Install Python Dependencies
        run: uv sync --frozen --extra tests

      - name: Run pylint
        run: |
          uv run pylint ./ddbj_search_converter

      - name: Run mypy
        run: |
          uv run mypy ./ddbj_search_converter

      - name: Run isort
        run: |
          uv run isort --check-only ./ddbj_search_converter
